npm install --save-dev hardhat
npx hardhat init 


todo list:
willl need a token faucet, so probably need to alter the token supply to infinity
plug in APRs
dashboard for "my models"
need a withdraw UI
