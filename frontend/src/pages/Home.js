import { useState, useEffect } from "react";
import { connectWallet, getContract } from "../utils/web3";
import { getDeployedContract } from "../utils/web3";
import { CONTRACT_ADDRESS, CONTRACT_ABI } from "../utils/contract";


const Home = () => {
    useEffect(() => {

    }, []);

    return <div>Home</div>;
};

export default Home;      